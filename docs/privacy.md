# Privacy Notes

脚本只在浏览器本地运行。它会读取当前页面 DOM，并在批量导出时用当前登录态请求学习通作业列表和作业详情页。

脚本不会主动把作业内容、答案、账号信息或 Cookie 上传到第三方服务器。

以下文件不应提交到公开仓库：

- `*.har`
- 导出的 `*.zip`
- 导出的 `*.pdf`
- 保存下来的课程页面 `*.html`
- 任何包含课程、账号、Cookie、URL 参数或答案内容的文件
